[project]
name = "llm-night-watch"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "httpx>=0.27.0",
  "pydantic>=2.8.0",
  "uvicorn>=0.30.6",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
